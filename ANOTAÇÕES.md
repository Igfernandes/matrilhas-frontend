# Geral:

- **O select precisa vir sofrer uma mudança para vir com uma caixa de filtro entre os elementos visualizados.**
- **O select precisa atender ao visual no figma**
- **Criar componente para mudança de status com a pegada melhor tipo toggle switch**
- Mudar a nomenclatura de ClientsMessage para ClientsDispatchers (DEPOIS)
- **O menu não fica fixo com base na navegação**
- **Não tem os recuos de página no topo, com a setinha pra voltar.**
- Corrigir todos os logs do console. (DEPOIS)
- Otimizar nomenclatura de tudo no projeto (DEPOIS)
- Refatorar código. (DEPOIS)
- Melhorar a proposta pros envios. Eles devem acontecer de background, de forma que não seja necessário esperar para enviar
- **Criar sistema de envio para atualizar notificações visualizadas**
- Criar uma API para o websocket. 
- **Lógica de Notificações não funciona**
- Lógica de Permissões não funciona.
- Otimizar todas as mensagens  de resposta.
- Tabela calculando o valor de exibição errado
- O snackbars estão sem icones corretos do lado
- **Fazer com que a ordem de exibição dos arquivos sejam contrária (DESC)**

# Clientes:

- **Selecionar todos precisa sair do checked quando não houver nada checado.**
- **O campo obrigatório deve criar a necessidade de obrigatoriedade nos grupos quando for definido.**
- **Deve haver um ícone de edição no lugar do ícone atual de bonequinho do lado do nome do cliente.**

# Gestão de Envios:

- **Deverá tornar obrigatório selecionar um destino de envio.**
- **Na hora de deletar, o botão de delete não está com o loading.**
- **Adicionar a coluna "send_at" na tabela.**
- **O contador de vinculados está somando errado.**
- INTEGRAR O INSTAGRAM (DEPOIS)
- Enviar para todos não funciona.

# Formulários:

- **Possibilidade de escolher um modelo já existente, para que ele seja usado como base inicial**
  - Corrigir os bugs de exibição para tornar mais fluído.
- **Criar nomenclaturas mais claras sobre os valores de preenchimento no modal.**
- **Criar área de visualizar formulários preenchidos**
- **Criar validações de campos obrigatório ou não. Eles não estão sendo respeitados.**
- **Corrigir o fluxo de exclusão do formulário. Está cm texto e outras coisas incorretas**
- **Área de edição não está funcionando**
- **Copiar link está vindo com o link errado.**
- **Testar o save das informações**

# Agenda:

- **Melhorar a proposta da exibição dos títulos na data.**
- **Verificar porque os usuários selecionados não estão já aparecendo selecionados na edição.**
- **Exibir as datas apenas para os usuários selecionados.**
- **Criar lógica de atualização de agendamentos**

# Financeiro:

- A cobrança sempre deverá ter o valor do preço maior do que o promocional
- **Trocar expiração da cobrança por dias, no lugar de data.**
- **As cobranças recorrentes não precisam ter quantidade.**

# Grupo de Usuários:

- **A edição de usuários não está funcionando**
- **O telefone do usuário não quer atualizar.**
- As permissões não está aparecendo já selecionadas.

# Configurações:

- **A senha está validando e retornando mensagens genéricas.**

# Gerenciamento API:

- **Os chats estão atualizando em conjunto.**
- **Colocar o título da integração selecionada**
